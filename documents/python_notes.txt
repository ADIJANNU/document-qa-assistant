## Day 3 — Lists and Loops

Lists are ordered, changeable collections in Python. An empty list is created with [].
Use list.append(value) to add an item to the end of a list.
Use a for loop to go through each item in a list one at a time.
Lists can hold mixed data types, and you can access any item using its index, starting at 0.